load("@bazel_gazelle//:def.bzl", "gazelle", "gazelle_binary")

package(
    default_visibility = ["//:__subpackages__"],
)

# Marked manual to avoid building in CI, which fails due to copt settings.
gazelle_binary(
    name = "gazelle_bin",
    languages = [
        "@bazel_skylib_gazelle_plugin//bzl",
    ],
    tags = ["manual"],
)

# Marked manual to avoid building in CI, which fails due to copt settings.
gazelle(
    name = "gazelle",
    gazelle = ":gazelle_bin",
    tags = ["manual"],
)
