(toolchain) Fixed a crash in `py_test` main validation
(`validate_test_main`) on Windows: the interpreter used for the check
couldn't find its DLLs or its stdlib once relocated
([#4079](https://github.com/bazel-contrib/rules_python/pull/4079)).
