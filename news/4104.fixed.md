(bootstrap) Fixed CPython interpreter symlink resolution during site
initialization leaking repository cache or execution root paths into
{obj}`sys.path` and {obj}`sys.base_prefix`, ensuring standard library and
runtime files resolve to their runfiles locations
([#4104](https://github.com/bazel-contrib/rules_python/pull/4104)).
