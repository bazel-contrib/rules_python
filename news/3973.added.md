(cc) Added experimental {obj}`py_extension` macro for creating C/C++ Python
extension modules.
(cc) Added `abi_flags`, `abi_tag`, `libc`, `platform_machine`, `platform_tag`,
and `sys_platform` attributes and info fields to {obj}`py_cc_toolchain` /
{obj}`PyCcToolchainInfo`.
