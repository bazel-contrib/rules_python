(pypi) Ported internal venv shebang and wheel {obj}`RECORD` rewriters from
shell scripts to Python ({obj}`py_binary`) to eliminate dependencies on host
coreutils and `awk` in `PATH`, fixing wheel installation on non-FHS systems like
NixOS ([#4125](https://github.com/bazel-contrib/rules_python/pull/4125)).
